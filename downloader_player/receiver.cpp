#include <QApplication>
#include <QDesktopWidget>
#include <QDir>

//#include "receiver.h"

int receiver(int argc, char *argv[])
{
    //QApplication app(argc, argv);
/**
    private:
    QQueue<QUrl> downloadLinkQueue;

    void addUrlToQueue(QUrl link)
    {
        downloadLinkQueue.enqueue(link);
    };

*/
    return 0;
}
